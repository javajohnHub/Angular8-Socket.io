{
  "name": "friday",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd ../client && ng build --prod && cd ../server && node ./bin/www"
  },
  "dependencies": {
    "body-parser": "~1.19.0",
    "dotenv": "^8.0.0",
    "express": "~4.17.1",
    "socket.io": "^2.4.0"
  }
}
